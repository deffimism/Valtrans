<Application x:Class="Valtrans.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
  <Application.Resources>
    <!-- Static faces keep regular/semibold/bold consistent across WPF renderers. -->
    <FontFamily x:Key="AppFontFamily">pack://application:,,,/Assets/Fonts/#Pretendard</FontFamily>
    <LinearGradientBrush x:Key="PageBrush" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#080D18" Offset="0"/>
      <GradientStop Color="#0D1526" Offset="0.58"/>
      <GradientStop Color="#10162A" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="PanelBrush" Color="#121B2E"/>
    <SolidColorBrush x:Key="PanelBorderBrush" Color="#27354D"/>
    <SolidColorBrush x:Key="TextBrush" Color="#F8FAFC"/>
    <SolidColorBrush x:Key="MutedBrush" Color="#94A3B8"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#A855F7"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#C084FC"/>
    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,0">
      <GradientStop Color="#6366F1" Offset="0"/>
      <GradientStop Color="#A855F7" Offset="0.5"/>
      <GradientStop Color="#EC4899" Offset="1"/>
    </LinearGradientBrush>
    <Style x:Key="CardBorder" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="16"/>
    </Style>
    <Style TargetType="Window">
      <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="{StaticResource PageBrush}"/>
      <Setter Property="UseLayoutRounding" Value="True"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
      <Setter Property="TextOptions.TextRenderingMode" Value="Grayscale"/>
      <Setter Property="TextOptions.TextHintingMode" Value="Fixed"/>
    </Style>
    <!-- Text must inherit the owning control's foreground, including content generated at runtime. -->
    <Style TargetType="TextBlock"/>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="Background" Value="#202C43"/>
      <Setter Property="BorderBrush" Value="#33445F"/>
      <Setter Property="Padding" Value="14,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1" CornerRadius="9" Padding="{TemplateBinding Padding}">
              <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"
                         HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="NoWrap"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Border" Property="Background" Value="#2B3A56"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Border" Property="Opacity" Value="0.82"/></Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="Background" Value="#F8FAFC"/>
                <Setter TargetName="Border" Property="BorderBrush" Value="#CBD5E1"/>
                <Setter Property="Foreground" Value="#64748B"/>
                <Setter Property="Cursor" Value="Arrow"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="{StaticResource AccentGradientBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style TargetType="ComboBox">
      <Style.Resources>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#0F172A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#0F172A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#0F172A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#E2E8F0"/>
        <Style TargetType="TextBlock">
          <Setter Property="Foreground" Value="#0F172A"/>
        </Style>
      </Style.Resources>
      <Setter Property="Background" Value="#F8FAFC"/>
      <Setter Property="Foreground" Value="#0F172A"/>
      <Setter Property="TextElement.Foreground" Value="#0F172A"/>
      <Setter Property="BorderBrush" Value="#CBD5E1"/>
      <Setter Property="Padding" Value="10,7"/>
      <Setter Property="MinHeight" Value="34"/>
    </Style>
    <Style TargetType="ComboBoxItem">
      <Setter Property="Background" Value="#F8FAFC"/>
      <Setter Property="Foreground" Value="#0F172A"/>
      <Setter Property="TextElement.Foreground" Value="#0F172A"/>
      <Setter Property="Padding" Value="8,6"/>
      <Style.Triggers>
        <Trigger Property="IsHighlighted" Value="True">
          <Setter Property="Background" Value="#E2E8F0"/>
          <Setter Property="Foreground" Value="#0F172A"/>
          <Setter Property="TextElement.Foreground" Value="#0F172A"/>
        </Trigger>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#DDE5F0"/>
          <Setter Property="Foreground" Value="#0F172A"/>
          <Setter Property="TextElement.Foreground" Value="#0F172A"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="ComboText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#0F172A"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="White"/>
      <Setter Property="Foreground" Value="#0F172A"/>
      <Setter Property="BorderBrush" Value="#CBD5E1"/>
      <Setter Property="Padding" Value="9,7"/>
      <Setter Property="CaretBrush" Value="#0F172A"/>
      <Setter Property="SelectionBrush" Value="#C7D2FE"/>
    </Style>
    <Style TargetType="PasswordBox">
      <Setter Property="Background" Value="White"/>
      <Setter Property="Foreground" Value="#0F172A"/>
      <Setter Property="BorderBrush" Value="#CBD5E1"/>
      <Setter Property="Padding" Value="9,7"/>
      <Setter Property="CaretBrush" Value="#0F172A"/>
      <Setter Property="SelectionBrush" Value="#C7D2FE"/>
    </Style>
    <Style TargetType="ProgressBar">
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
      <Setter Property="Background" Value="#0B1220"/>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="#CBD5E1"/>
    </Style>
    <Style TargetType="Slider">
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
    </Style>
  </Application.Resources>
</Application>
